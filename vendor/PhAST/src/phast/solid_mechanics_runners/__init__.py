"""Promoted solid-mechanics runner implementations.

These modules back the public YAML-first examples. Files under
``examples/solid_mechanics_beta`` are compatibility wrappers and curated artifacts;
the reusable implementation lives here so it is importable through PhAST.
"""
